#import * from "./Soumission.graphql"

type Query {
  soumission(id: ID!): Soumission!
  soumissions: [Soumission!]!
}

type Subscription {
  soumettre(exercice: ID!, code: String!): EtatSoumission! @acces
}
