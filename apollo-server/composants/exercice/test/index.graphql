#import * from "./Test.graphql"

type Mutation {

  creerTest(exercice: ID!, test: CreationTest!): Test!
  @acces(requis: [ GESTION_EXERCICE ])

  editerTest(id: ID!, modifications: EditionTest!): Test!
  @acces(requis: [ GESTION_EXERCICE ])

  supprimerTest(id: ID!): ID!
  @acces(requis: [ GESTION_EXERCICE ])

}

input CreationTest {
  nom: String!
  entree: String! = ""
  sortie: String!
}

input EditionTest {
  nom: String
  entree: String
  sortie: String
}